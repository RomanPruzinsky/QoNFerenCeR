package tr.qonferencer.shared.enums

/** Custom element type to render */
enum class FieldType {
	TEXT,
	SELECT,
	NUMBER,
	BOOL,
	DATE,
}
// TODO: doriesit input vs display, ci to bude display-only alebo podporovat aj input
